# DefaultGuard
End-to-end credit risk model that predicts loan defaults, handles class imbalance (SMOTE vs class weights), and tunes the decision threshold using a false-negative/false-positive business cost tradeoff.
